[tool.poetry]
name = "token_original_id"
version = "0.1.0"
description = ""
authors = ["settler"]
readme = "README.md"
packages = [{include = "token_original_id"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
